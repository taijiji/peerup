protocols {
    bgp {
        group {{ if_name }} {
            family inet {
                unicast;
            }
            type external;
            neighbor {{ neighbor_addr }} {
                description {{ neighbor_description }};
                peer-as {{ neighbor_asnum }};
            }
        }
    }
}

